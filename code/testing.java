package code;

import java.util.ArrayList;
import java.util.HashSet;
import java.util.LinkedList;
import java.util.Queue;
import java.util.Set;

import javax.sound.midi.Track;

public class testing {
    

    public static void main(String[] args) throws CloneNotSupportedException {
        // Set<TrackRecord> test = new HashSet<TrackRecord>();
        // TrackRecord p =new TrackRecord(new Pair(0, 0), 10);
        // TrackRecord h = new TrackRecord(new Pair(1, 1), 10);
        // boolean result=false;
        // test.add(p);
        // test.add(new TrackRecord(new Pair(1, 1), 10));
        // test.add(new TrackRecord(new Pair(0, 0), 20));

        // System.out.println(test.contains(p));
        // System.out.println(test.contains(new TrackRecord(new Pair(0, 0), 10)));
        // for(TrackRecord a : test) {
        //     if(a) {
        //         result=true;
        //         break;
        //     }
        //   }
        //   System.out.println(result+ "jjj");
        // System.out.println(test.contains(new TrackRecord(new Pair(0, 0), 20)));
    }
}
